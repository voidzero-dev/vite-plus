# create_org_invalid_manifest

## `vp create @your-org --no-interactive`

invalid manifest -> schema error

**Exit code:** 1

```
@your-org/create: createConfig.templates[0].template must be a non-empty string
```
