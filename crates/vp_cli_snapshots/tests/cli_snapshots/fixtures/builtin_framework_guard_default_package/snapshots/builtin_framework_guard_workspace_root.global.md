# builtin_framework_guard_workspace_root

## `cd framework_root_ws && vp dev`

a framework app at a workspace root refuses instead of listing member packages

**Exit code:** 1

```
VITE+ - The Unified Toolchain for the Web

error: this project uses Nuxt (nuxt.config.ts). `vp dev` runs the bundled Vite CLI, not the Nuxt CLI.
hint: did you mean `vp run dev`?
```
