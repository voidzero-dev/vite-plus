# placement_and_path_precedence

## `bash placement.sh`

Scoped changes move tool families together; refreshing preserves choices and PATH selects the first matching tool.

```
Scoped placement, refresh, shell cache, and PATH precedence passed
```
