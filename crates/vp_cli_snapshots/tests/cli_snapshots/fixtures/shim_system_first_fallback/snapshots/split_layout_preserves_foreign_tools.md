# split_layout_preserves_foreign_tools

## `sh split.sh`

Fallback shims stay in the data root when the main bin directory is shared, and refresh preserves foreign executables.

```
Split layout and foreign tool preservation passed
```
