# foreign_manager_falls_back_without_recursion

## `sh recursion.sh`

A foreign manager falling back to Vite+ reaches managed Node even with an inherited tool marker.

```
Foreign manager fallback terminates with managed Node
```
