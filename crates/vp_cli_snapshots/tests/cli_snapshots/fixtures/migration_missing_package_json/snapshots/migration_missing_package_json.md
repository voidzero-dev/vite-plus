# migration_missing_package_json

## `vp migrate --no-interactive --no-hooks --no-agent`

An empty directory gets an actionable project error

**Exit code:** 1

```
VITE+ - The Unified Toolchain for the Web

Cannot migrate .: no package.json found. Run vp migrate from a project root or pass its path explicitly.
```
