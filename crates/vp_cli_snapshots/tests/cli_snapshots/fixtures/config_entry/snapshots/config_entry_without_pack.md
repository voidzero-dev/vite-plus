# config_entry_without_pack

The published app config entry keeps native task loading and strict TypeScript 7 configuration without optional pack integrations.

## `vp install --ignore-scripts`


## `vp run verify`

```
VITE+ - The Unified Toolchain for the Web

$ node verify.mjs
App configuration is strict without pack; ESM, CommonJS, and native plugins retain identity
```
