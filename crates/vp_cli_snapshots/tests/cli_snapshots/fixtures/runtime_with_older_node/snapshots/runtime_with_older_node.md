# runtime_with_older_node

## `vp exec node --version`

.node-version pins Node 20.13.0 (below the declared engines floor) and still runs

```
VITE+ - The Unified Toolchain for the Web

<version>
```
