# command_env_doctor_shared_package_manager_mode

## `node print-doctor-configuration.cjs`

doctor keeps one package-manager row when every mode matches

```
Configuration
  ✓ Node.js           managed mode
  ✓ Package manager   managed mode
```
