# env_exec_resolves_target_project

## `node assert-env-exec-selection.cjs project`

Explicit env exec resolves the target directory instead of reusing the parent Node

```
env exec project: <version>
```
