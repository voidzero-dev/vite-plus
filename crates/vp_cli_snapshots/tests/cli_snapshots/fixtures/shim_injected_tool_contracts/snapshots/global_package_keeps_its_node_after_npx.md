# global_package_keeps_its_node_after_npx

## `vp install -g --node 22.18.0 ./runtime-probe`


## `npx --offline --call injected-runtime-probe`

Inherited bundled npm must not replace the global package's selected Node

```
The global package and its Node children use Node <version>
```
