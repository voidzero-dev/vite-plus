# toolchain_why_hint

## `vp why vite`

a successful human package query points to toolchain inspection

```

Vite+ also provides vite@8.2.0 through its toolchain.
Run `vp toolchain vite` to inspect bundled versions and relationships.
```

## `vp why vite --json`

machine-readable package-manager output has no hint

```
```
