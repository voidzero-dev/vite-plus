# toolchain_why_hint

## `vp why vite`

readable vp why output shows the toolchain hint

```

Vite+ also provides vite@<version> through its toolchain.
Run `vp toolchain vite` to show these versions and relationships.
```

## `vp why vite --json`

JSON package output does not include the hint

```
```
