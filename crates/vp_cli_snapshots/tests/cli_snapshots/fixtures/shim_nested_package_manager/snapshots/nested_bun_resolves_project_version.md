# nested_bun_resolves_project_version

## `vpt json-edit package.json packageManager bun@1.3.11`


## `vp env on bun`


## `npx --offline --call 'bun --version'`

A nested shim installs the project Bun on first use


## `npx --offline --call 'bun --version'`

npx resolves the project Bun without a system Bun

```
1.3.11
```
