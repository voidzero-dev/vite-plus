# prepared_home_matches_cli_setup

## `node verify.mjs`

The prepared home has the same preferences, environment files, and shims as setup followed by enabling package-manager management.

```
Prepared preferences, environment files, and shims match the CLI setup sequence.
Configuration changes remain isolated to their own home.
```
