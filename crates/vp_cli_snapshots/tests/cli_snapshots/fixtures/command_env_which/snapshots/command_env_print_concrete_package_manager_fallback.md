# command_env_print_concrete_package_manager_fallback

## `VP_HOME=${workspace}/custom home VP_SHELL=fish vp env print pnpm`

a concrete family falls back to its latest release and Fish keeps a spaced install path intact

```
VITE+ - The Unified Toolchain for the Web

# Add to your shell to use this environment for this session:
set -gx PATH "<workspace>/custom home/package_manager/pnpm/<version>/pnpm/bin" $PATH
```
