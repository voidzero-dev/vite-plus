# command_env_print_npm_uses_standalone_fallback

## `vp env print npm`

an explicit npm scope uses the standalone registry fallback

```
VITE+ - The Unified Toolchain for the Web

# Add to your shell to use this environment for this session:
export PATH="<home>/.vite-plus/package_manager/npm/<version>/npm/bin:$PATH"
```
