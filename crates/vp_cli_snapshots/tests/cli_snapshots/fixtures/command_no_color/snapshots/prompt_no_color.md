# prompt_no_color

## `vp create`

The picocolors-backed picker remains interactive without color

**→ expect-milestone:** `select:select:0`

```
VITE+ - The Unified Toolchain for the Web

  › Vite+ Monorepo: Create a new Vite+ monorepo project
    Vite+ Application: Create vite applications
    Vite+ Library: Create vite libraries
```

**← write-key:** `ctrl-c`

```
VITE+ - The Unified Toolchain for the Web

  Vite+ Monorepo

Operation cancelled
```
