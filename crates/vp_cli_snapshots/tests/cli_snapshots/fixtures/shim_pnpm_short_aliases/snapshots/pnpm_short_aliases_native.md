# pnpm_short_aliases_native

## `vpt json-edit package.json packageManager pnpm@12.0.0`


## `pn --version`


## `node assert-aliases.cjs 12.0.0`

Short aliases also dispatch through native pnpm and its dlx wrapper.

```
pn and pnx use the selected pnpm and dlx binaries
```
