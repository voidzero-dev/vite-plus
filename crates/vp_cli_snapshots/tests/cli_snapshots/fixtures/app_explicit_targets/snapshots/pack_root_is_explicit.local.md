# pack_root_is_explicit

The pack `--root` option is explicit command intent. Vite+ runs the command in the current directory.

## `vp pack --root src`

**Exit code:** 1

```
error: No input files, try "vp pack <your-file>" or create src/index.ts
```
