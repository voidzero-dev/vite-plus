# node_api_and_reports

## `node verify.mjs`

```
resolveConfig returns Vite config with .test and integration plugins
collect staticParse=true: collection side effects skipped
collect staticParse=false: collection side effects executed
JSON report written to <workspace>/.vitest/json/output.json
blob report written to <workspace>/.vitest/blob/blob.json
JUNIT report written to <workspace>/.vitest/junit/output.xml
JSON, JUnit, blob, attachments, artifacts, worker IDs, class mock prototypes, and the bench fixture passed
JSON report written to <workspace>/merged.json
blob merge preserved all four test results
```
