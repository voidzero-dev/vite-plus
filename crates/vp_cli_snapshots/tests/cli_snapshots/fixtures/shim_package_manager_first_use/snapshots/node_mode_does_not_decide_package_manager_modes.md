# node_mode_does_not_decide_package_manager_modes

## `vpt rm -f $VP_HOME/config.json`


## `vp env off node`


## `vpt print-file $VP_HOME/config.json`

a Node-only mode change writes only the Node mode

```
{
  "nodeShimMode": "system_first"
}
```
