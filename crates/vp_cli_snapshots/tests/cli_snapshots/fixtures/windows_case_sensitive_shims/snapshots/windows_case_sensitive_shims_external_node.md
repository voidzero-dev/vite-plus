# windows_case_sensitive_shims_external_node

## `node setup.cjs`


## `node local-cli.cjs`

The local CLI resolves the lowercase shim when real Node starts with uppercase PATHEXT.

```
local shim --version
```
