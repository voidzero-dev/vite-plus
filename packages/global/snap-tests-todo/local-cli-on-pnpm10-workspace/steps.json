{
  "ignoredPlatforms": [],
  "env": {
    "VITE_DISABLE_AUTO_INSTALL": "1"
  },
  "commands": [
    "vp fmt && cat package.json # should fmt in workspace root and auto add vite-plus to devDependencies",
    "cd packages/utils && vp fmt && cat package.json # should fmt in specific package and not add vite-plus to devDependencies"
  ]
}
