{
  "name": "@my-vite-plus-monorepo/lib",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "lint": "oxlint; node -e \"import('node:fs').then(({watch}) => watch('.', {recursive: true}, () => require('child_process').spawnSync('oxlint', [], {stdio:'inherit'})));\""
  },
  "devDependencies": {
    "oxlint": "^0.16.5",
    "vitest": "^3.2.2"
  }
}
