{
  "ignoredPlatforms": ["win32"],
  "env": {
    "VITE_DISABLE_AUTO_INSTALL": "1"
  },
  "commands": [
    "vite unlink -h # should show help message",
    "mkdir -p ../unlink-test-lib && echo '{\"name\": \"unlink-test-lib\", \"version\": \"1.0.0\"}' > ../unlink-test-lib/package.json # create test library",
    "vite link ../unlink-test-lib && cat package.json # link the library first",
    "vite unlink unlink-test-lib && cat package.json # should unlink the package",
    "vite link ../unlink-test-lib # link again",
    "vite unlink && cat package.json # should unlink all packages"
  ]
}
