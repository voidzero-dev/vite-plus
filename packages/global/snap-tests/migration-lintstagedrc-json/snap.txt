[2]> vp migrate -h # migration help message
error: unrecognized subcommand 'migrate'

  tip: a similar subcommand exists: 'i'

Usage: vite <COMMAND>

For more information, try '--help'.

[2]> vp migrate --no-interactive # migration work with lintstagedrc.json
error: unrecognized subcommand 'migrate'

  tip: a similar subcommand exists: 'i'

Usage: vite <COMMAND>

For more information, try '--help'.

> cat .lintstagedrc.json # check lintstagedrc.json
{
  "*.js": "oxlint --fix"
}

> cat package.json # check package.json
{
  "name": "migration-lintstagedrc",
  "lint-staged": {
    "*.@(js|ts|tsx|yml|yaml|md|json|html|toml)": [
      "oxfmt --staged",
      "eslint --fix"
    ],
    "*.@(js|ts|tsx)": [
      "oxlint --fix"
    ]
  }
}
