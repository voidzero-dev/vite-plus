{
  "env": {
    "VITE_DISABLE_AUTO_INSTALL": "1"
  },
  "commands": [
    "vite pm cache --help # should show help",
    "vite pm cache dir > /dev/null # should show cache directory (uses pnpm store path)",
    "vite pm cache path > /dev/null # should show cache path (alias for dir, uses pnpm store path)"
  ]
}
