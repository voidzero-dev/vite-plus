{
  "ignoredPlatforms": ["win32"],
  "env": {
    "VITE_DISABLE_AUTO_INSTALL": "1"
  },
  "commands": [
    "vite pm cache dir # should show cache directory (uses npm config get cache)",
    "vite pm cache path # should show cache path (alias for dir, uses npm config get cache)"
  ]
}
