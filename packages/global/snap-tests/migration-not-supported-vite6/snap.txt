> vp install # install dependencies first
[2]> vp migrate --no-interactive # migration should fail because vite version is not supported
error: unrecognized subcommand 'migrate'

  tip: a similar subcommand exists: 'i'

Usage: vite <COMMAND>

For more information, try '--help'.

> cat package.json # check package.json is not updated
{
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "packageManager": "pnpm@<semver>"
}