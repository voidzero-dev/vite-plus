{
  "env": {
    "VITE_DISABLE_AUTO_INSTALL": "1"
  },
  "commands": [
    "vite pm view testnpm2 # should view testnpm2 package information (uses npm view)",
    "vite pm view testnpm2 version # should view testnpm2 version field (uses npm view)"
  ]
}
