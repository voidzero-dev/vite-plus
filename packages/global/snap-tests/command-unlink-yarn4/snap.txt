> mkdir -p ../unlink-test-lib-yarn && echo '{"name": "unlink-test-lib-yarn", "version": "1.0.0"}' > ../unlink-test-lib-yarn/package.json # create test library
[2]> vp link ../unlink-test-lib-yarn && cat package.json # link the library first
error: unrecognized subcommand 'link'

  tip: some similar subcommands exist: 'i', 'lint'

Usage: vite <COMMAND>

For more information, try '--help'.

[2]> vp unlink unlink-test-lib-yarn && cat package.json # should unlink the package
error: unrecognized subcommand 'unlink'

Usage: vite <COMMAND>

For more information, try '--help'.

[129]> vp link ../unlink-test-lib-yarn && cat package.json # link again
error: unrecognized subcommand 'link'

  tip: some similar subcommands exist: 'i', 'lint'

Usage: vite <COMMAND>

For more information, try '--help'.

[2]> vp unlink --recursive && cat package.json # should unlink all with --all flag
error: unrecognized subcommand 'unlink'

Usage: vite <COMMAND>

For more information, try '--help'.

[2]> vp unlink -r && cat package.json # should work with -r short form
error: unrecognized subcommand 'unlink'

Usage: vite <COMMAND>

For more information, try '--help'.
