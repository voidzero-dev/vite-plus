{
  "env": {
    "VITE_DISABLE_AUTO_INSTALL": "1"
  },
  "commands": [
    {
      "command": "vp install # install dependencies first",
      "ignoreOutput": true
    },
    "vp migration # migration should fail because vitest version is not supported",
    "cat package.json # check package.json is not updated"
  ]
}
