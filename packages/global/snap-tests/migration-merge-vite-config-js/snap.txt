[2]> vp migrate --no-interactive # migration should merge vite.config.js and remove oxlintrc
error: unrecognized subcommand 'migrate'

  tip: a similar subcommand exists: 'i'

Usage: vite <COMMAND>

For more information, try '--help'.

> cat vite.config.js # check vite.config.js
import react from '@vitejs/plugin-react';

export default {
  plugins: [react()],
};

[1]> cat .oxlintrc.json && exit 1 || true # check .oxlintrc.json is removed
{
  "rules": {
    "no-unused-vars": "error"
  }
}

> cat package.json # check package.json
{
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build",
    "lint": "oxlint"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "oxlint": "1",
    "vite": "^7.0.0"
  }
}
