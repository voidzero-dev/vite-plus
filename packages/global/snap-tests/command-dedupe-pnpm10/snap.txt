[2]> vp dedupe --help # should show help
error: unrecognized subcommand 'dedupe'

Usage: vite <COMMAND>

For more information, try '--help'.

[2]> vp dedupe && cat package.json # should dedupe dependencies
error: unrecognized subcommand 'dedupe'

Usage: vite <COMMAND>

For more information, try '--help'.

[2]> vp dedupe --check && cat package.json # should check if deduplication would make changes
error: unrecognized subcommand 'dedupe'

Usage: vite <COMMAND>

For more information, try '--help'.

[129]> vp dedupe -- --loglevel=warn && cat package.json # support pass through arguments
error: unrecognized subcommand 'dedupe'

Usage: vite <COMMAND>

For more information, try '--help'.

[2]> json-edit package.json '_.dependencies = {}' && cat package.json && vp dedupe --check # should check fails because no dependencies
{
  "name": "command-dedupe-pnpm10",
  "version": "1.0.0",
  "dependencies": {},
  "devDependencies": {
    "test-vite-plus-package": "1.0.0"
  },
  "optionalDependencies": {
    "test-vite-plus-package-optional": "1.0.0"
  },
  "packageManager": "pnpm@<semver>"
}
error: unrecognized subcommand 'dedupe'

Usage: vite <COMMAND>

For more information, try '--help'.

[2]> vp dedupe && cat package.json && vp dedupe --check # should dedupe fix the change by removing the dependencies
error: unrecognized subcommand 'dedupe'

Usage: vite <COMMAND>

For more information, try '--help'.
