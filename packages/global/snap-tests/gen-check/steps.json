{
  
  "commands": [
    "vite gen --help # show help",
    "vite gen --list # list templates",
    "vite gen --no-interactive # run in non-interactive mode without template name will show error"
  ]
}
