{
  "env": {
    "VITE_DISABLE_AUTO_INSTALL": "1"
  },
  "commands": [
    "vite pm publish --dry-run -- --loglevel error # should preview publish without actually publishing (uses npm publish --dry-run)"
  ]
}
