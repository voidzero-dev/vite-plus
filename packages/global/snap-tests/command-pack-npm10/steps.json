{
  "ignoredPlatforms": ["win32"],
  "env": {
    "VITE_DISABLE_AUTO_INSTALL": "1"
  },
  "commands": [
    "vite pm pack --json && rm -rf *.tgz # should pack current package",
    "vite pm pack --pack-destination ./dist --json && rm -rf ./dist # should pack with destination",
    "vite pm pack --json -- --loglevel=warn && rm -rf *.tgz # should support pass through arguments"
  ]
}
