{
  "ignoredPlatforms": ["win32"],
  "env": {
    "VITE_DISABLE_AUTO_INSTALL": "1"
  },
  "commands": [
    "vp dlx --help # should show help message",
    "vp dlx -s cowsay hello # should fallback to npx for yarn@1"
  ]
}
