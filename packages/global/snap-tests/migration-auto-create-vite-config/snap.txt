[2]> vp migrate --no-interactive # migration should auto create vite.config.ts and remove oxlintrc and oxfmtrc
error: unrecognized subcommand 'migrate'

  tip: a similar subcommand exists: 'i'

Usage: vite <COMMAND>

For more information, try '--help'.

[1]> cat vite.config.ts # check vite.config.ts
cat: vite.config.ts: No such file or directory

[1]> cat .oxlintrc.json && exit 1 || true # check .oxlintrc.json is removed
{
  "rules": {
    "no-unused-vars": "error"
  }
}

[1]> cat .oxfmtrc.json && exit 1 || true # check .oxfmtrc.json is removed
{
  "printWidth": 100,
  "tabWidth": 2,
  "semi": true,
  "singleQuote": true,
  "trailingComma": "es5"
}

> cat package.json # check package.json
{
  "devDependencies": {
    "oxfmt": "1",
    "oxlint": "1"
  }
}
