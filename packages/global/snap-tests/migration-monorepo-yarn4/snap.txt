[2]> vp migrate --no-interactive # migration should merge vite.config.ts and remove oxlintrc
error: unrecognized subcommand 'migrate'

  tip: a similar subcommand exists: 'i'

Usage: vite <COMMAND>

For more information, try '--help'.

> cat vite.config.ts # check vite.config.ts
import react from '@vitejs/plugin-react';
import { defineConfig } from 'vite';

export default defineConfig({
  plugins: [react()],
});

[1]> cat .oxlintrc.json && exit 1 || true # check .oxlintrc.json is removed
{
  "rules": {
    "no-unused-vars": "error"
  }
}

> cat package.json # check package.json
{
  "name": "migration-monorepo-yarn4",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "test:run": "vitest run",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest --watch",
    "test": "vitest",
    "lint": "oxlint",
    "fmt": "oxfmt"
  },
  "dependencies": {
    "testnpm2": "1.0.0"
  },
  "devDependencies": {
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  },
  "packageManager": "yarn@<semver>"
}

[1]> cat .yarnrc.yml # check .yarnrc.yml
cat: .yarnrc.yml: No such file or directory

> cat packages/app/package.json # check app package.json
{
  "name": "app",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "test": "vitest"
  },
  "dependencies": {
    "@vite-plus-test/utils": "workspace:*",
    "test-vite-plus-install": "1.0.0",
    "testnpm2": "catalog:"
  },
  "devDependencies": {
    "test-vite-plus-package": "1.0.0",
    "vite": "catalog:",
    "vitest": "catalog:"
  },
  "optionalDependencies": {
    "test-vite-plus-other-optional": "1.0.0"
  }
}

> cat packages/utils/package.json # check utils package.json
{
  "name": "@vite-plus-test/utils",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "test": "vitest"
  },
  "dependencies": {
    "testnpm2": "1.0.0"
  },
  "devDependencies": {
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
