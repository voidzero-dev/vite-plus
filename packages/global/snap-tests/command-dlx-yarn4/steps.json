{
  "env": {
    "VITE_DISABLE_AUTO_INSTALL": "1"
  },
  "commands": [
    "vite dlx --help # should show help message",
    "vite dlx -s cowsay hello # should run cowsay with yarn dlx",
    "vite dlx -s cowsay@1.6.0 hello # should run specific version"
  ]
}
