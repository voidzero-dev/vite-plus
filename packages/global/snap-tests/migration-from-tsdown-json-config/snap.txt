[2]> vp migrate --no-interactive # migration should rewrite imports to vite-plus
error: unrecognized subcommand 'migrate'

  tip: a similar subcommand exists: 'i'

Usage: vite <COMMAND>

For more information, try '--help'.

[1]> cat tsdown.config.json && exit 1 || true # check tsdown.config.json should be removed
{
  "entry": "src/index.ts",
  "outDir": "dist",
  "format": ["esm", "cjs"],
  "dts": true,
  "inputOptions": {
    "cwd": "./src"
  }
}

> cat vite.config.ts # check vite.config.ts
import { defineConfig } from 'vite-plus';

export default defineConfig({
  server: {
    port: 3000,
  },
});

> cat package.json # check package.json
{
  "name": "migration-from-tsdown-json-config",
  "scripts": {
    "build": "tsdown",
    "build:watch": "tsdown --watch",
    "build:dts": "tsdown --dts"
  },
  "devDependencies": {
    "tsdown": "^0.5.0",
    "vite": "^7.0.0"
  }
}

[2]> vp migrate --no-interactive # run migration again to check if it is idempotent
error: unrecognized subcommand 'migrate'

  tip: a similar subcommand exists: 'i'

Usage: vite <COMMAND>

For more information, try '--help'.

> cat vite.config.ts # check vite.config.ts
import { defineConfig } from 'vite-plus';

export default defineConfig({
  server: {
    port: 3000,
  },
});

> cat package.json # check package.json
{
  "name": "migration-from-tsdown-json-config",
  "scripts": {
    "build": "tsdown",
    "build:watch": "tsdown --watch",
    "build:dts": "tsdown --dts"
  },
  "devDependencies": {
    "tsdown": "^0.5.0",
    "vite": "^7.0.0"
  }
}
