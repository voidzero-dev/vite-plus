{
  "ignoredPlatforms": ["win32"],
  "env": {
    "VITE_DISABLE_AUTO_INSTALL": "1"
  },
  "commands": [
    "vite pm pack # should pack current package",
    "vite pm pack --out ./dist/package.tgz # should pack with output file",
    "vite pm pack --json # should pack with json output",
    "vite pm pack -- --dry-run # should support pass through arguments"
  ]
}
