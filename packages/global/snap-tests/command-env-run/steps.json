{
  "env": {},
  "commands": [
    "vp env run --node 20 node -v # Run node with specific major version",
    "vp env run --node 20 node -e \"console.log('Hello from Node ' + process.version)\" # Run inline script"
  ]
}
