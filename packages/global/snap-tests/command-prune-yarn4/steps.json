{
  "env": {
    "VITE_DISABLE_AUTO_INSTALL": "1"
  },
  "commands": [
    "vite pm prune --help # should show help",
    "vite pm prune # should show warning that yarn does not support prune command"
  ]
}
