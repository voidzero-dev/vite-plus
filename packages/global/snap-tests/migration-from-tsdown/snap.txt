[129]> vp migrate --no-interactive # migration should rewrite imports to vite-plus
error: unrecognized subcommand 'migrate'

  tip: a similar subcommand exists: 'i'

Usage: vite <COMMAND>

For more information, try '--help'.

> cat tsdown.config.ts # check tsdown.config.ts
import { defineConfig } from 'tsdown';

export default defineConfig({
  entry: 'src/index.ts',
  outDir: 'dist',
  format: ['esm', 'cjs'],
  dts: true,
});

[1]> cat vite.config.ts # check vite.config.ts
cat: vite.config.ts: No such file or directory

> cat package.json # check package.json
{
  "name": "migration-from-tsdown",
  "scripts": {
    "build": "tsdown",
    "build:watch": "tsdown --watch",
    "build:dts": "tsdown --dts"
  },
  "devDependencies": {
    "tsdown": "^0.5.0",
    "vite": "^7.0.0"
  }
}

[129]> vp migrate --no-interactive # run migration again to check if it is idempotent
error: unrecognized subcommand 'migrate'

  tip: a similar subcommand exists: 'i'

Usage: vite <COMMAND>

For more information, try '--help'.

> cat tsdown.config.ts # check tsdown.config.ts
import { defineConfig } from 'tsdown';

export default defineConfig({
  entry: 'src/index.ts',
  outDir: 'dist',
  format: ['esm', 'cjs'],
  dts: true,
});

[1]> cat vite.config.ts # check vite.config.ts
cat: vite.config.ts: No such file or directory

> cat package.json # check package.json
{
  "name": "migration-from-tsdown",
  "scripts": {
    "build": "tsdown",
    "build:watch": "tsdown --watch",
    "build:dts": "tsdown --dts"
  },
  "devDependencies": {
    "tsdown": "^0.5.0",
    "vite": "^7.0.0"
  }
}
