{
  "ignoredPlatforms": ["win32", "darwin"],
  "env": {
    "VITE_DISABLE_AUTO_INSTALL": "1"
  },
  "commands": [
    "vite pm cache dir # should show cache directory (uses yarn config get cacheFolder)",
    "vite pm cache path # should show cache path (alias for dir, uses yarn config get cacheFolder)"
  ]
}
