{
  "ignoredPlatforms": ["win32"],
  "env": {
    "VITE_DISABLE_AUTO_INSTALL": "1"
  },
  "commands": [
    "vite install",
    "vite why testnpm2 --filter app # should check why in specific workspace using --workspace",
    "vite why test-vite-plus-package --filter app # should check why dev dependencies in app workspace",
    "vite why testnpm2 --filter app --json # should support json output with workspace filter"
  ]
}
