{
  "commands": [
    "vp migrate --no-interactive # migration should create .husky/pre-commit even without .git",
    "cat package.json # check package.json has prepare script and lint-staged config",
    "test -d .husky && echo 'husky dir exists' || echo 'no husky dir'",
    "cat .husky/pre-commit # pre-commit hook should exist even without .git"
  ]
}
