{
  "commands": [
    "vp migrate --no-interactive # migration preserves inline arrays in an existing bunfig.toml",
    "cat bunfig.toml # check peer suppression is added without corrupting inline arrays"
  ]
}
