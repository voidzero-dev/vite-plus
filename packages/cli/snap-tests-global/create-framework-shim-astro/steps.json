{
  "ignoredPlatforms": ["win32"],
  "localVitePlusPackages": true,
  "env": {},
  "commands": [
    {
      "command": "node $SNAP_SHARED_DIR/mock-npm-registry.mjs -- vp create astro --no-interactive -- my-astro-app --yes --skip-houston --template basics # create Astro app",
      "ignoreOutput": true,
      "timeout": 120000
    },
    "cat my-astro-app/src/env.d.ts # check Astro shim",
    {
      "command": "cd my-astro-app && node $SNAP_SHARED_DIR/mock-npm-registry.mjs -- vp install --ignore-scripts -- --no-frozen-lockfile # install dependencies",
      "ignoreOutput": true,
      "timeout": 120000
    },
    {
      "command": "cd my-astro-app && sed -i.bak -e '/jsPlugins/d' -e '/rules:/d' -e '/options:/d' vite.config.ts && vp check --fix # fix generated formatting and ensure no errors"
    }
  ]
}
