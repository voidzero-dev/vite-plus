> vp env doctor > doctor.txt 2>&1; node -e "const t = require('node:fs').readFileSync('doctor.txt', 'utf8'); const need = ['devEngines', '.node-version (20.18.0) does not satisfy devEngines.runtime', 'npm@10.5.0', 'but devEngines.packageManager requires']; for (const s of need) { if (!t.includes(s)) { console.error('missing line in doctor output: ' + s); process.exit(1); } } console.log('doctor reports devEngines conflicts');" # doctor surfaces the .node-version vs devEngines.runtime and packageManager vs devEngines.packageManager conflicts
doctor reports devEngines conflicts
