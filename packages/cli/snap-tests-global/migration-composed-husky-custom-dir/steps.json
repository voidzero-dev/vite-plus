{
  "commands": [
    { "command": "git init", "ignoreOutput": true },
    "vp migrate --no-interactive # migration should preserve custom husky dir in composed prepare",
    "cat package.json # prepare should be 'vp config && npm run build'",
    "cat .vite-hooks/pre-commit # pre-commit hook should be in custom dir"
  ]
}
