{
  "env": {},
  "commands": [
    "vp migrate --no-interactive # existing bun Vite+ catalog project gains a direct vite edge for bun#8406",
    "cat package.json # the direct vite edge is catalog: (resolving through the bun catalog), not a concrete alias"
  ]
}
