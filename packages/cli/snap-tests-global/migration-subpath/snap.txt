> git init
> vp migrate foo --no-interactive # migration work with subpath
┌  VITE+ - The Unified Toolchain for the Web
│
●  Using default package manager: pnpm
│
●  pnpm@latest installing...
│
●  pnpm@<semver> installed
│
◆  ✔ Git hooks configured
│
◆  Wrote agent instructions to AGENTS.md
│
└  ✔ Migration completed!


> cat foo/package.json # check package.json
{
  "name": "migration-subpath",
  "scripts": {
    "prepare": "vp prepare"
  },
  "devDependencies": {
    "vite-plus": "latest"
  },
  "packageManager": "pnpm@<semver>",
  "pnpm": {
    "overrides": {
      "vite": "npm:@voidzero-dev/vite-plus-core@latest",
      "vitest": "npm:@voidzero-dev/vite-plus-test@latest"
    }
  },
  "vite-staged": {
    "*.@(js|ts|tsx|yml|yaml|md|json|html|toml)": [
      "vp fmt --staged",
      "eslint --fix"
    ],
    "*.@(js|ts|tsx)": [
      "vp lint --fix"
    ]
  }
}

> cat foo/.husky/pre-commit # pre-commit hook should exist in subpath
vp staged

> git config --local core.hooksPath # should be foo/.husky/_
foo/.husky/_

> cat foo/.husky/_/h | grep 'node_modules/.bin' # should resolve from hook file location
export PATH="$d/node_modules/.bin:$PATH"
