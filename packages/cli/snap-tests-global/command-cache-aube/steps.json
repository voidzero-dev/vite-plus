{
  "commands": [
    "vp pm cache --help # should show help",
    "vp pm cache dir > /dev/null # should show cache directory (uses aube store path)",
    "vp pm cache path > /dev/null # should show cache path (alias for dir, uses aube store path)"
  ]
}
