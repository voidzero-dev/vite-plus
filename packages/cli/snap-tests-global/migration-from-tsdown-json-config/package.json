{
  "name": "migration-from-tsdown-json-config",
  "scripts": {
    "build": "tsdown",
    "build:watch": "tsdown --watch",
    "build:dts": "tsdown --dts"
  },
  "devDependencies": {
    "tsdown": "^0.21.0",
    "vite": "^7.0.0"
  }
}
