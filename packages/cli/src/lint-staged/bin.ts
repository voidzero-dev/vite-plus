// Runs lint-staged on staged files.
// Delegates to lint-staged's CLI entry point for full argument support.
// Bundled by rolldown — no runtime dependency needed in user projects.
import 'lint-staged/bin';
