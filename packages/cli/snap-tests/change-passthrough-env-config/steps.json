{
  "env": {
    "VITE_DISABLE_AUTO_INSTALL": "1"
  },
  "commands": [
    "MY_ENV=1 vite run hello",
    "MY_ENV=2 vite run hello # MY_ENV is pass-through. should hit the cache created in step 1",
    "json-edit vite-task.json '_.tasks.hello.passThroughEnvs.push(\"MY_ENV2\")' # add a new pass through env",
    "MY_ENV=2 vite run hello # cache should be invalidated because passThroughEnvs config changed"
  ]
}
