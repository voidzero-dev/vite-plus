{
  "ignoredPlatforms": ["win32"],
  "env": {
    "VITE_DISABLE_AUTO_INSTALL": "1"
  },
  "commands": [
    "vite run script1 # initial run, create the cache",
    "vite run script2 # script2 has the same command as script1, should hit the cache",
    "echo baz > foo.txt # change the input file to invalidate the cache",
    "vite run script2 # script2 should report a cache miss, and update the cache",
    "vite run script1 # script1 should hit the updated cache"
  ]
}
