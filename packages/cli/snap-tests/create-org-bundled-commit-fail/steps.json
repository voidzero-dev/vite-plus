{
  "env": {
    "GIT_CONFIG_COUNT": "1",
    "GIT_CONFIG_KEY_0": "core.hooksPath",
    "GIT_CONFIG_VALUE_0": "../hooks"
  },
  "commands": [
    "node $SNAP_CASES_DIR/.shared/mock-npm-registry.mjs -- vp create @your-org:demo --no-interactive --directory my-demo-app --git # failing pre-commit hook (via core.hooksPath) aborts the initial commit; git output is surfaced",
    "test -d my-demo-app/.git && echo 'Git initialized' # repository still created despite failed commit"
  ]
}
