{
  "ignoredPlatforms": ["win32", "darwin", "linux"],
  "env": {
    "VITE_DISABLE_AUTO_INSTALL": "1"
  },
  "commands": [
    "vite run doc # build documentation",
    "vite run doc # build documentation again should hit the cache"
  ]
}
