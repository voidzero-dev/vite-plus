{
  "ignoredPlatforms": ["win32"],
  "env": {
    "VITE_DISABLE_AUTO_INSTALL": "1"
  },
  "commands": [
    "vite lib -h # should print the help message",
    "vite run lib # should build the library",
    "ls dist # should have the library",
    "vite run lib # should hit cache"
  ]
}
