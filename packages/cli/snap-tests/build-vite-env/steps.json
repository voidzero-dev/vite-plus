{
  "ignoredPlatforms": ["win32"],
  "env": {
    "VITE_DISABLE_AUTO_INSTALL": "1"
  },
  "commands": [
    "VITE_MY_VAR=1 vite run build",
    "VITE_MY_VAR=1 vite run build # should hit cache",
    "VITE_MY_VAR=2 vite run build # env changed, should miss cache"
  ]
}
