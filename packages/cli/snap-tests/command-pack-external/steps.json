{
  "commands": [
    "vp pack --deps.neverBundle node:path src/index.ts # should bundle with deps.neverBundle flag",
    "vp pack --external node:path src/index.ts # should bundle with legacy external flag"
  ]
}
